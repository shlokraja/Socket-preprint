{
  "name": "socket_connection",
  "version": "1.0.0",
  "description": "example application for socket connectivity in node.js",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^1.4.8"
  }
}
